<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ranking de Jugadores</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/ranking-historial.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/header.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/footer.css">
</head>
<body>
{{> header}}
<div style="height: 50px;"></div>
<h1 class="text-center">Ranking de Jugadores</h1>
<div class="container">
    <h2>Ranking General</h2>
    <table class="table">
        <thead>
        <tr>
            <th>Posición</th>
            <th>Nombre de Usuario</th>
            <th>Puntaje Total</th>
        </tr>
        </thead>
        <tbody>
        {{#usuarios}}
            <tr>
                <td>{{posicion}}</td>
                <td>
                    <a href="/TPFinalPreguntas/ranking/verPerfilJugador?id={{id_usuario}}">
                    {{nombre_usuario}}
                    </a>
                </td>
                <td>{{puntaje_total}}</td>
            </tr>
        {{/usuarios}}
        {{^usuarios}}
            <tr>
                <td colspan="3">No hay usuarios en el ranking.</td>
            </tr>
        {{/usuarios}}
        </tbody>
    </table>

    <nav aria-label="Paginación del ranking">
        <ul class="pagination justify-content-center">
            {{#prev_page}}
                <li class="page-item">
                    <a class="page-link" href="/TPFinalPreguntas/ranking/show?pagina={{prev_page}}" aria-label="Página anterior">
                    <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
            {{/prev_page}}
            {{^prev_page}}
                <li class="page-item disabled">
                    <span class="page-link">&laquo;</span>
                </li>
            {{/prev_page}}

            {{#pages}}
                <li class="page-item {{active}}">
                    <a class="page-link" href="/TPFinalPreguntas/ranking/show?pagina={{numero}}">{{numero}}</a>
                </li>
            {{/pages}}

            {{#next_page}}
                <li class="page-item">
                    <a class="page-link" href="/TPFinalPreguntas/ranking/show?pagina={{next_page}}" aria-label="Página siguiente">
                    <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            {{/next_page}}
            {{^next_page}}
                <li class="page-item disabled">
                    <span class="page-link">&raquo;</span>
                </li>
            {{/next_page}}
        </ul>
    </nav>

</div>

<div class="container">
    <h2>Ranking por País</h2>
    <table class="table">
        <thead>
        <tr>
            <th>Posición</th>
            <th>Nombre de Usuario</th>
            <th>Mejor Puntaje</th>
            <th>Partidas Jugadas</th>
        </tr>
        </thead>
        <tbody>
        {{#rankingPorPais}}
            <tr>
                <td>{{posicion_pais}}</td>
                <td>{{nombre_usuario}}</td>
                <td>{{mejor_puntaje}}</td>
                <td>{{partidas_jugadas}}</td>
            </tr>
        {{/rankingPorPais}}
        {{^rankingPorPais}}
            <tr>
                <td colspan="4">No hay usuarios en el ranking por país.</td>
            </tr>
        {{/rankingPorPais}}
        </tbody>
    </table>
</div>

<div class="container">
    <h2>Ranking por Ciudad</h2>
    <table class="table">
        <thead>
        <tr>
            <th>Posición</th>
            <th>Nombre de Usuario</th>
            <th>Mejor Puntaje</th>
            <th>Partidas Jugadas</th>
        </tr>
        </thead>
        <tbody>
        {{#rankingPorCiudad}}
            <tr>
                <td>{{posicion_ciudad}}</td>
                <td>{{nombre_usuario}}</td>
                <td>{{mejor_puntaje}}</td>
                <td>{{partidas_jugadas}}</td>
            </tr>
        {{/rankingPorCiudad}}
        {{^rankingPorCiudad}}
            <tr>
                <td colspan="4">No hay usuarios en el ranking por ciudad.</td>
            </tr>
        {{/rankingPorCiudad}}
        </tbody>
    </table>
</div>
<div style="height: 50px;"></div>
{{> footer}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>