<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Preguntas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/listar.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/header.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/footer.css">
</head>
<body>
{{> header}}
<div style="height: 80px;"></div>
<div class="container">
    <h1 class="h3 mb-4 titulo-listado">Listado de Preguntas</h1>

    {{#preguntas}}
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">{{contenido}}</h5>
                <p class="card-text">Categoría: {{categoria}}</p>
                <p class="card-text">Dificultad: {{nivel_dificultad}}</p>
                <p class="card-text">Opciones: A) {{opcion_a}}, B) {{opcion_b}}, C) {{opcion_c}}, D) {{opcion_d}}</p>
                <p class="card-text">Respuesta Correcta: {{respuesta_correcta}}</p>
            </div>
        </div>
    {{/preguntas}}

    {{^preguntas}}
        <p>No hay preguntas disponibles.</p>
    {{/preguntas}}

    <!-- Paginación -->
    <div class="d-flex justify-content-between mt-4">
        {{#prevPage}}
            <a href="/TPFinalPreguntas/pregunta/listar&pagina={{prevPage}}" class="btn btn-primary">Anterior</a>
        {{/prevPage}}

        {{#nextPage}}
            <a href="/TPFinalPreguntas/pregunta/listar&pagina={{nextPage}}" class="btn btn-primary">Siguiente</a>
        {{/nextPage}}
    </div>

</div>
<div style="height: 80px;"></div>
{{> footer}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>