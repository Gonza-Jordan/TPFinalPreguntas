<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Revisar Preguntas Sugeridas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/home.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/header.css">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/footer.css">
</head>
<body>
{{> header}}
<div class="content-wrapper">
    <div class="container">
        <h1 class="my-4">Revisar Preguntas Reportadas</h1>

        {{#mensaje}}
            <div class="alert alert-success">{{mensaje}}</div>
        {{/mensaje}}

        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Contenido</th>
                <th>Categoría</th>
                <th>Nivel de Dificultad</th>
                <th>Opciones</th>
                <th>Respuesta Correcta</th>
                <th>Comentario</th>
                <th>Acciones</th>
            </tr>
            </thead>
            <tbody>
            {{#preguntasReportadas}}
                <tr>
                    <td>{{contenido}}</td>
                    <td>{{categoria}}</td>
                    <td>{{nivel_dificultad}}</td>
                    <td>
                        A) {{opcion_a}}<br>
                        B) {{opcion_b}}<br>
                        C) {{opcion_c}}<br>
                        D) {{opcion_d}}
                    </td>
                    <td>{{respuesta_correcta}}</td>
                    <td>{{comentario}}</td>
                    <td>
                        <a href="pregunta/habilitarPreguntaReportada?id={{id_pregunta}}" class="btn btn-success">Habilitar</a>
                        <a href="pregunta/deshabilitarPreguntaReportada?id={{id_pregunta}}" class="btn btn-danger">Deshabilitar</a>
                    </td>
                </tr>
            {{/preguntasReportadas}}
            {{^preguntasReportadas}}
                <tr>
                    <td colspan="7" class="text-center">No hay preguntas reportadas pendientes.</td>
                </tr>
            {{/preguntasReportadas}}
            </tbody>
        </table>
    </div>
</div>
{{> footer}}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
