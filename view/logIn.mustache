<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/TPFinalPreguntas/public/css/logIn-signUp.css">
</head>
<body style="height: 100vh;">
<div class="container">
    <h2 class="text-center mb-4">Iniciar Sesión</h2>

    <?php if (!empty($mensaje)): ?>
    <div class="text-center">
        <?php echo $mensaje; ?>
    </div>
    <?php endif; ?>
    {{#alert_success}}
        <div class="alert alert-success">{{alert_success}}</div>
    {{/alert_success}}

    {{#alert_error}}
        <div class="alert alert-danger">{{alert_error}}</div>
    {{/alert_error}}

    <form action="/TPFinalPreguntas/auth/login" method="POST">
        <div class="mb-3">
            <label for="username" class="form-label">Nombre de usuario</label>
            <input type="text" class="form-control" id="username" name="username" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">Contraseña</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Iniciar Sesión</button>
    </form>
    <div class="text-center mt-3">
        <p>¿No tienes una cuenta? <a href="/TPFinalPreguntas/auth/registro">Regístrate</a></p>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
