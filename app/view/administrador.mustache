<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Administrador</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="public/css/header.css">
    <link rel="stylesheet" href="public/css/footer.css">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .admin-view {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 900px;
            width: 100%;
        }

        h1, h2, h3 {
            text-align: center;
            margin-bottom: 20px;
        }

        .stats, .filter, .user-stats, .user-gender-stats, .user-age-stats, .trampitas-stats {
            margin-bottom: 30px;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        ul li {
            padding: 10px;
            background-color: #e9ecef;
            border-radius: 5px;
            margin-bottom: 10px;
            font-weight: bold;
        }

        select {
            display: block;
            margin: 0 auto;
            padding: 10px;
            width: 50%;
            border-radius: 5px;
            border: 1px solid #ced4da;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            background-color: #fff;
        }

        table, th, td {
            border: 1px solid #dee2e6;
        }

        th, td {
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        #imprimir-btn {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        #imprimir-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
<main class="admin-view">
    {{> header}}
    <h1>Panel de Administrador</h1>

    <div class="stats">
        <h2>Estadísticas Generales</h2>
        <ul>
            <li>Cantidad de jugadores: {{totalJugadores}}</li>
            <li>Cantidad de partidas jugadas: {{totalPartidas}}</li>
            <li>Cantidad de preguntas en el juego: {{totalPreguntas}}</li>
            <li>Cantidad de preguntas creadas: {{preguntasCreadas}}</li>
            <li>Usuarios nuevos: {{usuariosNuevos}}</li>
            <li>Porcentaje de preguntas respondidas correctamente: {{porcentajeRespuestasCorrectas}}%</li>
        </ul>
    </div>

    <div class="filter">
        <h3>Filtrar por:</h3>
        <select>
            <option value="dia">Día</option>
            <option value="semana">Semana</option>
            <option value="mes">Mes</option>
            <option value="año">Año</option>
        </select>
    </div>

    <div class="user-stats">
        <h3>Usuarios por País</h3>
        <table>
            <thead>
            <tr>
                <th>País</th>
                <th>Cantidad de Usuarios</th>
            </tr>
            </thead>
            <tbody>
            {{#usuariosPorPais}}
                <tr>
                    <td>{{pais}}</td>
                    <td>{{cantidadUsuarios}}</td>
                </tr>
            {{/usuariosPorPais}}
            </tbody>
        </table>
    </div>

    <div class="user-gender-stats">
        <h3>Usuarios por Sexo</h3>
        <table>
            <thead>
            <tr>
                <th>Sexo</th>
                <th>Cantidad de Usuarios</th>
            </tr>
            </thead>
            <tbody>
            {{#usuariosPorSexo}}
                <tr>
                    <td>{{sexo}}</td>
                    <td>{{cantidadUsuarios}}</td>
                </tr>
            {{/usuariosPorSexo}}
            </tbody>
        </table>
    </div>

    <div class="user-age-stats">
        <h3>Usuarios por Grupo de Edad</h3>
        <table>
            <thead>
            <tr>
                <th>Grupo de Edad</th>
                <th>Cantidad de Usuarios</th>
            </tr>
            </thead>
            <tbody>
            {{#usuariosPorEdad}}
                <tr>
                    <td>{{grupoEdad}}</td>
                    <td>{{cantidadUsuarios}}</td>
                </tr>
            {{/usuariosPorEdad}}
            </tbody>
        </table>
    </div>

    <div class="trampitas-stats">
        <h3>Balance de Trampitas</h3>
        <p>Total de trampitas vendidas: {{totalTrampitas}}</p>
        <p>Ganancias con trampitas: ${{gananciasTrampitas}}</p>
    </div>

    <button id="imprimir-btn">Imprimir Reporte</button>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
