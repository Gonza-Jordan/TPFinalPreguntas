<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil Jugador</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../public/css/perfil-perfilJugador.css">
</head>
<body>
<div class="perfil-jugador">
    <header>
        <button class="back-button">&lt;</button>
        <h1>{{nombre_usuario}}</h1>
        <p>Puntaje: {{puntaje_total}}</p>
    </header>
    <form>
        <label for="fecha-nacimiento">Fecha de nacimiento</label>
        <input type="text" id="fecha-nacimiento" value="{{anio_nacimiento}}">

        <label for="sexo">Sexo</label>
        <select id="sexo">
            <option value="M" {{#sexoM}}selected{{/sexoM}}>Masculino</option>
            <option value="F" {{#sexoF}}selected{{/sexoF}}>Femenino</option>
            <option value="O" {{#sexoO}}selected{{/sexoO}}>Otro</option>
        </select>

        <label for="ubicacion">Ubicación</label>
        <textarea id="ubicacion">{{ciudad}}, {{pais}}</textarea>
    </form>

    <section class="historial-partidas">
        <h2>Historial de partidas</h2>
        {{#partidas}}
            <div class="partida">
                <p>{{fecha_partida}} - Puntaje: {{puntaje_obtenido}}</p>
                <span>{{estado}}</span>
            </div>
        {{/partidas}}
    </section>
</div>

</body>
