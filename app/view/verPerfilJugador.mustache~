<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ranking de Jugadores</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="public/css/ranking-historial.css">
</head>
<body>

<h2>Perfil de {{usuario.nombre_usuario}}</h2>
<p><strong>Puntaje Total:</strong> {{usuario.puntaje_total}}</p>
<p><strong>Ubicación:</strong> {{usuario.ciudad}}, {{usuario.pais}}</p>
<p><strong>Partidas Realizadas:</strong></p>
<ul>
    {{#partidas}}
        <li>{{.}}</li>
    {{/partidas}}
</ul>
<button class="qr-button">QR</button>
<div id="mapa"></div>

</body>
<script>

    function initMap() {
        // Coordenadas de la ciudad del usuario
        var location = { lat: /*latitud*/, lng: /*longitud*/ };
        var map = new google.maps.Map(document.getElementById('mapa'), {
            zoom: 10,
            center: location
        });
        var marker = new google.maps.Marker({
            position: location,
            map: map
        });
    }
</script>
