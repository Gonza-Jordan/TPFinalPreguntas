<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Historial de Partidas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="public/css/historial.css">
    <link rel="stylesheet" href="public/css/home.css">
    <link rel="stylesheet" href="public/css/header.css">
    <link rel="stylesheet" href="public/css/footer.css">
</head>
<body>
{{> header}}
<div class="container mt-5" style="padding-top: 200px">

    <h2 class="text-center" style="margin-top: 300px">Historial de Partidas</h2>
    <div class="historial-container mt-4">
        {{#partidas}}
            <div class="card partida-card mb-3 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title">Partida  {{id_partida}}</h5>
                        <p class="card-text">
                            <strong>Fecha:</strong> {{horario_inicio}}<br>
                            <strong>Puntuación:</strong> {{puntos_sumados}}
                        </p>
                    </div>
                </div>
            </div>
        {{/partidas}}
        {{^partidas}}
            <p class="text-center">No se encontraron partidas en el historial.</p>
        {{/partidas}}
    </div>
</div>

{{> footer}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
